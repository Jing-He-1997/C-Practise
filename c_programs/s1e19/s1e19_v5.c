#include <stdio.h>
#include <string.h>
int main()
{
	char str1[]="HE JING";
	char str2[]="JING";

	strcmp(str1,str2);	//当此函数检测到两个字符串中的某个对应字符不同时，函数
				//停止并返回str1[当前位置]-str2[当前位置]
				
	printf("%d\n", a);

	return 0;
}
